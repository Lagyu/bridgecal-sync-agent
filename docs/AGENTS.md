# docs/ — Agent Instructions

Edits in `docs/` should:
- Prefer short paragraphs and concrete checklists.
- Use exact CLI commands and file paths.
- Avoid vague language (“should”, “maybe”) unless uncertainty is real.
- Keep docs consistent with `docs/requirements.md` (the spec).
